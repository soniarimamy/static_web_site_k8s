apiVersion: v1
kind: Service
metadata:
  name: static-web-service
  namespace: static-web
spec:
  type: NodePort
  selector:
    app: static-web-site
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
